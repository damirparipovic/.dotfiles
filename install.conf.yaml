- defaults:
    link:
      create: true
      force: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.tmux.conf.local: tmux.conf.local
    ~/.config/nvim: nvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
